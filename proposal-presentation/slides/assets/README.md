# Slide assets

Drop deck-only images here (screenshots, custom graphics). Chapter figures stay in
`assets/figures/` and are referenced via `../../assets/figures/...`.

## Here now
- **`timeline_1.png` / `timeline_2.png`** — the two schedule banners (Jun–Aug 3–5 defense;
  Aug 10–late December). Post-October dates are not yet fixed, so they render with a
  month-level label, a dashed stem and a hollow dot (`tentative=True` in the script).
  Generated by `src/visualization/presentation_timeline.py` from
  `Gantt Chart Final T7.xlsx`; edit that script and re-run it, do not hand-edit the PNGs.

## Needed now
- **`pipeline-placeholder.png`** — screenshot of the internship AI→`.psd` pipeline, used on
  the "Where This Started" origin slide (S1b). Best shot: AI-generated image beside the
  `.psd` / font mismatch, so the font gap is visible. Replace this filename or update the
  `![](assets/pipeline-placeholder.png)` tag in `../slides.md`.

Generic reference only — no employer name/branding on the slide (public repo).
